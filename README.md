# mistral.flow
Simple, state-aware inference proxy for recurrent model. It sits in front of `mistral.rs` to automatically save and restore user-specific model states, making it easier to build persistent AI applications.
